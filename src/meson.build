# openssl = dependency('libssl')
# crypto = dependency('libcrypto')
# subdir('ssl') # ssl_lib_static

threads = dependency('threads')
subdir('common')   # common_lib_static
subdir('server')
subdir('client')
